Setup environment:
Add environment:
Variable    | Value
MONGO_HOME    {mongo_path}

Step 1: start mongo
mongod --dbpath %MONGO_HOME%\data\db




shutdown mongo (open adminstrators cmd)
net stop MongoDB